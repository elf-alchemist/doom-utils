Better Doom on the command line.

Simple set of utility programs for interacting with Doom engine games, built on BASH.

To start developing, run 'make' for available tasks,
and run 'make tools' to list the necessary CLI tools to work.

